
# Start the conversation
chat_result = user_proxy.initiate_chat(
    node_{{ first_converser['id'] }},
    {%- if main_user_proxy.data.class in ['RetrieveUserProxyAgent', 'MathUserProxyAgent']  %}
    problem=args.message,
    {%- else %}
    message=args.message,
    {%- endif %}
    {%- if initialize_options.max_turns %}
    max_turns={{ initialize_options.max_turns }},
    {%- endif %}
    {%- if initialize_options.summary_method %}
    summary_method="{{ initialize_options.summary_method }}",
    {%- endif %}
)
